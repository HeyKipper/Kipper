# Database

Migrations applied to the Supabase project, mirrored here so the schema is
reviewable in git.

`20260922175834_create_waitlist_table` was applied before this directory
existed and is not mirrored; it lives in the Supabase migration history only.

After any schema change, re-run the linter and fix what it finds:
Supabase Dashboard → Advisors → Security.
